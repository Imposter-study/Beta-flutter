import 'package:flutter/material.dart';


class CreateAIScreen extends StatelessWidget {
  const CreateAIScreen({super.key});
  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('제작 화면'));
  }
}